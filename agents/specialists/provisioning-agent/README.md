# Provisioning Specialist Agent Scaffold

Scope:
- Provenance-safe provisioning state and route readiness.
- Ownership of `services/provisioning-truth` and its tests.

Allowed files:
- `services/provisioning-truth/**`
- `packages/contracts/**` for provisioning-related contracts.

Forbidden files:
- `services/relationship-state/**`
- `services/measurement/**`
- `services/delivery-runner/src/worker.js`
